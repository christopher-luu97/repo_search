const config = {
    basePath: "<Path to repositories on file network>",
    backendServer:"<Address where backend server is hosted. e.g. The Machine IP address>",
    modelAPI:"<Complete API link to where model is hosted for chatbot functionality>"
  }
  
  export default config;
  